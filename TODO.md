# TODO List: Сайт InfoSphere

## 🚀 Проект: Информационный портал на Laravel
**Стек:** Xampp, Node.js, Composer, Laravel, MySQL, Backpack, Bootstrap, JavaScript (fetch/axios)

## Этап 1: Инициализация и Настройка окружения

### 1.1. Установка и настройка проекта (статус: завершено)

## Этап 2: База данных и Миграции

### 2.1. Создание миграций (статус: завершено)

## Этап 3: Модели и Связи (Eloquent) (статус: завершено)

### 3.1. Создание и настройка моделей
-  User: Раскомментировать HasFactory, Notifiable. Добавить связи:
    - profile() (hasOne)
    - articles() (hasMany)
    - favoriteArticles() (belongsToMany)
    - likedArticles() (belongsToMany)
    - comments() (hasMany)
    - subscribedCommunities() (belongsToMany)
    - ownCommunities() (hasMany)
-  Profile: Создать модель php artisan make:model Profile. Связь user() (belongsTo).
-  Category: Создать модель. Связь articles() (hasMany).
-  Tag: Создать модель. Связь articles() (belongsToMany).
-  Community: Создать модель. Связи:
    - creator() (belongsTo User)
    - subscribers() (belongsToMany User)
    - articles() (hasMany)
-  Article: Создать модель. Связи:
    - user() (belongsTo)
    - category() (belongsTo)
    - community() (belongsTo)
    - tags() (belongsToMany)
    - comments() (hasMany)
    - likes() (hasMany)
    - favoritedByUsers() (belongsToMany User)
-  Comment: Создать модель. Связи:
    - user() (belongsTo)
    - article() (belongsTo)
    - replies() (hasMany, для вложенных комментариев)
    - parent() (belongsTo, для вложенных комментариев)
-  Favorite / Like: Создать модели (если нужны отдельно). Либо использовать belongsToMany без модели.

---

## Этап 4: Аутентификация и Профиль пользователя (Breeze) (статус: завершено)

### 4.1. Доработка аутентификации
-  Кастомизировать стандартные контроллеры Breeze (Register, Login, Password) под нужды проекта (добавить роль user по умолчанию при регистрации).
-  Создать сиды (Seeders) для создания тестового пользователя, админа, модератора.

### 4.2. Профиль пользователя
-  Создать контроллер ProfileController.
-  Создать роуты для профиля (/profile/{user}).
-  Создать Blade-шаблон для отображения профиля:
    -  Аватар, Имя, Bio.
    -  Список статей пользователя.
    -  Список сообществ, на которые подписан пользователь.
    -  Список избранных статей.
-  Реализовать редактирование профиля:
    -  Создать отдельную страницу (/profile/edit).
    -  Добавить возможность загрузки/смены аватара.
    -  Добавить поле Bio.
    -  Настроить валидацию через Form Request.
-  Реализовать смену пароля (использовать стандартный функционал Breeze).
-  Реализовать удаление аккаунта (с подтверждением).

---

## Этап 5: Админ-панель (Backpack) (статус: завершено)

### 5.1. Настройка CRUD для Администратора
-  Пользователи (Users): Создать CRUD php artisan backpack:crud user. Настроить поля для:
    -  Просмотра и редактирования всех полей.
    -  Добавить поле role (select из enum или из конфига).
    -  Добавить поле is_blocked (checkbox).
-  Категории (Categories): Создать CRUD. Поля: name, slug (автогенерация).
-  Теги (Tags): Создать CRUD. Поля: name, slug.
-  Сообщества (Communities): Создать CRUD. Поля: name, slug, description, cover_image, user_id (creator), is_private.
-  Статьи (Articles): Создать CRUD. Настроить:
    -  Поля для всех атрибутов (title, content, image, и т.д.).
    -  Выпадающие списки для user, category, community.
    -  Мультиселект для тегов.
    -  Возможность менять статус (draft/published).
-  Комментарии (Comments): Создать CRUD. Простой просмотр, возможность удалить.

---

## Этап 6: Основной функционал (Статьи, Категории, Теги) (статус: завершено)

### 6.1. Категории и Теги
-  Создать контроллеры: CategoryController (show), TagController (show).
-  Реализовать отображение списка статей по категории/тегу с пагинацией.

### 6.2. Статьи (Articles)
-  Создать контроллер ArticleController с методами: index, show, create, store, edit, update, destroy.
-  Создать роуты для статей (resource).
-  Создание/Редактирование статьи:
    -  Blade-форма с полями: title, category_id, community_id (nullable), tags (multiselect или text input), excerpt, content (textarea), image (file input), status (draft/published).
    -  Реализовать генерацию slug из title.
    -  Загрузка и обработка изображения.
    -  Валидация через Form Request.
    -  Использовать Policy для проверки: только автор может редактировать/удалить свою статью.
-  Просмотр статьи:
    -  Шаблон show.blade.php с отображением всей информации.
    -  Увеличивать счетчик views_count при каждом просмотре.
    -  SEO-мета-теги (title, description, keywords) на основе статьи.
    -  Блок с комментариями.
    -  Кнопки "Лайк" и "В избранное" (сделать JS позже).
-  Лента статей (Главная страница):
    -  ArticleController@index.
    -  Вывод статей со статусом published.
    -  Пагинация.
    -  Блок "Популярные статьи" (сортировка по просмотрам/лайкам).
    -  Блок "Новые сообщества".

---

## Этап 7: Сообщества (Communities) (статус: завершено)

### 7.1. Реализация сообществ
-  Создать контроллер CommunityController (resource).
-  Реализовать создание сообщества (только для авторизованных).
-  Страница просмотра сообщества:
    -  Обложка, описание, создатель, кол-во подписчиков.
    -  Лента статей этого сообщества.
    -  Кнопка "Подписаться / Отписаться" (сделать JS позже).
-  Реализовать логику подписки/отписки в контроллере (отдельный метод subscribe / unsubscribe).
-  Добавить middleware для проверки доступа в закрытое сообщество (is_private).

---

## Этап 8: Комментарии (Comments) (статус: завершено)
### 8.1. Система комментариев
-  Создать контроллер CommentController с методами store, update, destroy.
-  Реализовать форму добавления комментария к статье.
-  Реализовать отображение комментариев под статьей (с поддержкой вложенных ответов, если решено их делать).
-  Использовать Policy для редактирования/удаления комментария (только автор или админ/модер).
-  *JS-часть:* Добавление комментария через AJAX (опционально).

---

## Этап 9: Уведомления (Laravel Notifications) (статус: завершено)

### 9.1. Настройка уведомлений
-  Создать необходимые классы уведомлений:
    -  NewPostInCommunity
    -  NewCommentOnArticle
    -  ArticleLiked
-  Настроить отправку уведомлений в нужных местах кода:
    -  При создании новой статьи в сообществе (уведомление всем подписчикам).
    -  При добавлении комментария к статье (уведомление автору статьи).
    -  При лайке статьи (уведомление автору статьи).
-  Создать миграцию для таблицы notifications (поставляется с Laravel).
-  Создать контроллер NotificationController для работы с уведомлениями.
-  Добавить иконку/выпадающее меню уведомлений в основной шаблон (header).
-  Реализовать отображение непрочитанных уведомлений.
-  JS-функция: Пометка всех или одного уведомления как прочитанное без перезагрузки.

---

## Этап 10: JavaScript (Интерактивность без перезагрузки) (статус: завершено)

### 10.1. Настройка фронтенда
-  Подключить и настроить Axios (или использовать fetch).
-  Убедиться, что CSRF-токен передается в заголовках всех AJAX-запросов.

### 10.2. Реализация интерактивных функций
-  Лайки:
    -  Написать JS-обработчик на кнопку лайка.
    -  Отправка POST-запроса на /articles/{article}/like.
    -  Создать соответствующий маршрут и метод в контроллере.
    -  Обновить иконку (закрашенная/пустая) и счетчик лайков на странице.
-  Избранное:
    -  Аналогично лайкам, маршрут /articles/{article}/favorite.
    -  Обновление иконки "звездочка".
-  Подписка на сообщество:
    -  Обработчик на кнопку подписки на странице сообщества.
    -  Маршрут /communities/{community}/subscribe.
    -  Мгновенное обновление текста кнопки ("Подписаться" / "Отписаться") и счетчика подписчиков.
-  Уведомления:
    -  Обработчик клика на уведомление (перенаправление на пост/статью и пометка как прочитанное через AJAX).
    -  Кнопка "Отметить все как прочитанные".

---

## Этап 11: Поиск и Фильтрация (статус: завершено)

### 11.1. Реализация поиска
-  Реализовать поиск по названию статьи (LIKE %query%).
-  Интегрировать фильтрацию по категории и тегам через GET-параметры.
-  Реализовать сортировку (по дате, по популярности).
-  Страница результатов с пагинацией.

---

## Этап 12: Политики (Policies) и Middleware (статус: завершено)

### 12.1. Разграничение доступа
-  Создать ArticlePolicy:
    -  update / delete (только автор).
    -  view (черновики видит только автор или админ).
-  Создать CommentPolicy:
    -  update / delete (автор или модератор/админ).
-  Создать CommunityPolicy:
    -  update / delete (только создатель или админ).
-  Создать middleware для ролей: CheckRole (admin, moderator, user).
-  Зарегистрировать middleware в Kernel.php.
-  Применить middleware к соответствующим роутам (например, админка, редактирование статей).
